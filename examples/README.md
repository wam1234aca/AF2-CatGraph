# Examples

`minimal/config.template.yaml` is a path-only starting point. It intentionally
contains no structure ensemble, ConSurf archive, or GED binary.

Copy the template into a local project directory, rename it to `config.yaml`, replace every `REQUIRED` path, and run Preflight. Do not use placeholder values for a scientific run.
